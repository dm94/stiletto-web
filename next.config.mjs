/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export', // Outputs a Single-Page Application (SPA)
  distDir: './build', // Changes the output directory `./dist/`
}

export default nextConfig;